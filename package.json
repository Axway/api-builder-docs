{
  "name": "apibuilderPOC-open-docs",
  "version": "0.0.1",
  "description": "API Builder documentation",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Axway/apibuilderPOC-open-docs.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Axway/apibuilderPOC-open-docs/issues"
  },
  "homepage": "https://github.com/Axway/apibuilderPOC-open-docs",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^9.8.6",
    "grunt": "^1.3.0",
    "grunt-markdownlint": "^2.6.0",
    "minimist": "^1.2.5",
    "postcss-cli": "^5.0.1",
    "remark-cli": "^10.0.0",
    "remark-frontmatter": "^4.0.0",
    "remark-lint": "^8.0.0",
    "remark-lint-are-links-valid": "^1.0.3",
    "remark-preset-lint-consistent": "^4.0.0",
    "remark-preset-lint-markdown-style-guide": "^4.0.0",
    "remark-preset-lint-recommended": "^5.0.0",
    "spellchecker-cli": "^4.8.0"
  },
  "scripts": {
    "start": "npm run build:merge && cd build && mkdir -p public && hugo server",
    "build": "npm run build:merge && npm run build:lint && npm run build:spell",
    "build:lint": "remark --frail --quiet -r .remarkrc.yml content/en/docs/Plugins/*.md",
    "build:spell": "spellchecker",
    "clean": "rm -rf build node_modules",
    "build:merge": "./build.sh",
    "postinstall": "npm run build"
  }
}
